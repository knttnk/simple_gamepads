import 'package:flutter/foundation.dart';
import 'package:flutter_test/flutter_test.dart' as test;
import 'package:simple_gamepads/simple_gamepads.dart' as simple_gamepads;

void main() {
  // test.test("数のテスト", () async {
  //   final int numberOfGamepads = (await simple_gamepads.getAvailableGamepadIds()).length;
  //   test.expect(numberOfGamepads, test.greaterThan(0));
  // });

  // test.test("軸のテスト", () async {
  //   for (int i = 0; i < 4; i++) {
  //     final double? axisValue = simple_gamepads.getGamepadAxis(0, i);
  //     debugPrint("Axis $i: $axisValue");
  //     test.expect(axisValue, test.isNotNull);
  //   }
  // });

  // test.test("ボタンのテスト", () async {
  //   for (int i = 0; i < 16; i++) {
  //     final bool buttonValue = simple_gamepads.getGamepadButton(0, i);
  //     debugPrint("Button $i: $buttonValue");
  //     test.expect(buttonValue, test.isNotNull);
  //   }
  // });
}
